# Plan: Add proxy selection to temporary account checks

1. Extend shared/API types
   - Add a small request body type for temporary-account checks carrying an optional `RouteProxyConfig`.
   - Update `src/api.ts` so both “check all” and “check single account” POST endpoints can send the selected proxy mode.

2. Wire backend check requests through the selected proxy
   - Update `fetchTemporaryAccountCheckText()` to accept an optional proxy config.
   - When proxy mode is `system`, reuse the existing `routeProxy()` / `ProxyAgent` helpers so account checks really go through the detected system/SAMAPI proxy.
   - Keep `direct` as a supported mode and pass no dispatcher for direct requests.
   - Thread the proxy option through Codex token refresh, Codex usage checks, OpenAI API-key checks, check-all, check-by-id, and import auto-check paths.
   - Parse the POST body on `/api/temporary-accounts/check` and `/api/temporary-accounts/accounts/:id/check`.

3. Add detection-page UI control
   - Add component state for the temporary account check proxy mode.
   - Show a selector in the 临时账号 toolbar near “检查 GPT”: “检测代理：系统代理 / 直连”.
   - Send the selected mode when checking all accounts or refreshing a single account.
   - Use “系统代理” as the default so the current 127.0.0.1:7897 system proxy can be used immediately; users can switch to 直连 if needed.

4. Adjust layout styles
   - Make the new proxy selector fit the existing toolbar and stack cleanly on mobile.

5. Verify and restart service
   - Run `pnpm build`.
   - Per saved project instruction, restart with `brew services stop samapi` then `brew services start samapi` if the build succeeds.
